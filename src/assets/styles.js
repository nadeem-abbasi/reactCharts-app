import theme from './theme';

const styles = {
	graphCard: {
		margin: '25px auto',
		padding: 10
	},
	header: {
		borderBottom: `5px solid ${theme.palette.secondary.main}`,
		marginTop: 10
	}
};
export default styles;
